{% extends 'espaceMembres/layout.html.twig' %}

{% block content %}
    <h1>Session creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('membre_session_index') }}">Back to the list</a>
        </li>
    </ul>

{% endblock %}