{% extends 'espaceMembres/layout.html.twig' %}

{% block content %}
    <h1>Instruments list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for instrument in instruments %}
            <tr>
                <td><a href="{{ path('membre_instrument_show', { 'id': instrument.id }) }}">{{ instrument.id }}</a></td>
                <td>{{ instrument.libelle }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('membre_instrument_show', { 'id': instrument.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_instrument_edit', { 'id': instrument.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_new') }}">Create a new instrument</a>
        </li>
    </ul>
{% endblock %}
